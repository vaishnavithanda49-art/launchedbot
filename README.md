# launchedbot
AI Meeting Assistant is a Python-based application that listens to live meetings, converts speech to text using Google Speech API with an offline Whisper fallback, responds to important keywords, logs transcripts, and generates concise AI summaries using a transformer-based model.
